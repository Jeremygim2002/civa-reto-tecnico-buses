spring.application.name=bus-api

# Server Configuration
server.port=${SERVER_PORT:8080}

# Database Configuration (PostgreSQL - Supabase)
spring.datasource.url=jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.erauybasscflufufenkc
spring.datasource.password=jeremy
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.database=POSTGRESQL

# SQL script execution (disabled for PostgreSQL - data already in Supabase)
spring.sql.init.mode=never

# CORS Configuration
cors.allowed-origin=${APP_CORS_ALLOWED_ORIGIN:http://localhost:5173}

# Security
spring.security.user.name=${BASIC_AUTH_USERNAME:admin}
spring.security.user.password=${BASIC_AUTH_PASSWORD:admin123}
spring.security.user.roles=USER

# Logging
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web.cors=DEBUG
